PORT=5000
NODE_ENV=development

# JWT Secrets (change these in production!)
JWT_SECRET="kmc_jwt_secret_change_me_in_production"
JWT_REFRESH_SECRET="kmc_refresh_secret_change_me_in_production"
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Database (SQLite path for dev — swap with Postgres URI in prod)
DATABASE_URL = postgresql://postgres:Mohsin%40098765@db.txcqkgwyggegkvrtrxmo.supabase.co:5432/postgres
# DB_PATH=./data/kmc.sqlite

# Upload directory
UPLOAD_DIR=./uploads

# CORS — comma-separated allowed origins
CORS_ORIGINS=http://localhost:8080
